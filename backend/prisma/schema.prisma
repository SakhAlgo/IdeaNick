generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Idea {
  id        String   @id @default(cuid())
  nick      String   @unique
  name      String
  description String
  text      String
}