{
  "name": "ideanick",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @ideanick/webapp dev",
    "build": "pnpm --filter @ideanick/webapp build",
    "prettify": "prettier --log-level warn --cache --write '{src/**/*,*}.{ts,tsx,js,json,yml,scss}' '!**/{node_modules,dist}/**/*'"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {}
}
