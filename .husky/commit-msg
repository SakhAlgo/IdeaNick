#!/bin/sh
python3 - "$1" << 'EOF'
import sys

commit_msg_file = sys.argv[1]

with open(commit_msg_file, 'r', encoding='utf-8') as f:
    lines = f.readlines()

if lines:
    # Process first line: trim, capitalize, remove trailing dot
    first = lines[0].strip()
    if first:
        first = first[0].upper() + first[1:]
        if first.endswith('.'):
            first = first[:-1]
    lines[0] = first + '\n'
    
    # Trim other lines
    lines[1:] = [line.strip() for line in lines[1:] if line.strip()]

with open(commit_msg_file, 'w', encoding='utf-8') as f:
    f.writelines(lines)
EOF
